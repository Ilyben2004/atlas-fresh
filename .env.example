# Copy to .env in the repo root for docker compose / local runs.
# Leave empty for no-key mode: UI shows a labelled deterministic KPI summary
# (not AI-generated). Set a key to enable live Gemini answers + tool routing.

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
